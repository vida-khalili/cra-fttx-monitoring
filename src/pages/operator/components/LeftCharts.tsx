import { Box } from "@mui/material";
import React from "react";
import chartIcon from "src/assets/left-charts.svg";

const LeftCharts = () => (
  <Box component={"img"} width={"100%"} src={chartIcon} mt={2} />
);

export default LeftCharts;
