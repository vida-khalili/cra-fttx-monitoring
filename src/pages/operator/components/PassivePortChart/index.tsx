import Box from "@mui/material/Box";
import chart1 from "src/assets/operator-sample-chart.png";

const PassivePortChart = () => (
  <Box component={"img"} width={"100%"} src={chart1} mt={2} />
);

export default PassivePortChart;
